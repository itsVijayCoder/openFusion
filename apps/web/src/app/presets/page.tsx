import { PlaceholderPage } from "@/features/shell/placeholder-page";

export default function PresetsPage() {
  return <PlaceholderPage title="Presets" description="Team presets, same-provider preferences, panel size, timeout, and cost policy." />;
}
