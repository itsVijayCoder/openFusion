import { PlaceholderPage } from "@/features/shell/placeholder-page";

export default function ModelsPage() {
  return <PlaceholderPage title="Models" description="Installed, CLI-auth, API-key, and verified model availability." />;
}
