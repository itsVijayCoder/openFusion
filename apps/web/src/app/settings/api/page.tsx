import { PlaceholderPage } from "@/features/shell/placeholder-page";

export default function ApiSettingsPage() {
  return <PlaceholderPage title="API Settings" description="Team API keys and OpenAI-compatible endpoint configuration." />;
}
