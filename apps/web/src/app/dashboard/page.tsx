import { PlaceholderPage } from "@/features/shell/placeholder-page";

export default function DashboardPage() {
  return <PlaceholderPage title="Dashboard" description="Overview of runs, runners, models, safety events, and usage." />;
}
