import { PlaceholderPage } from "@/features/shell/placeholder-page";

export default function WorkspacesPage() {
  return <PlaceholderPage title="Workspaces" description="Allowed paths, default runners, permission profiles, and edit policy." />;
}
