import { PlaceholderPage } from "@/features/shell/placeholder-page";

export default function LoginPage() {
  return <PlaceholderPage title="Login" description="Cloudflare Access-backed entry point for internal team users." />;
}
