# UI Components

Shared, package-level UI components will live here once components need to be reused outside `apps/web`.

Keep shadcn-owned source components in the consuming app until reuse is justified.
